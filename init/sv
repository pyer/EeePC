#!/bin/sh
#
action=$1
service=$2

PATH=/sbin:/usr/sbin:/bin:/usr/bin

check_service() {
  if [ -z ${service} ]
  then
    echo "ERROR: service undefined"
    exit 1
  fi
}

check_enabled_service() {
  check_service
  if [ ! -d "/etc/svdir/enabled/${service}" ]
  then
    echo "ERROR: service ${service} is disabled"
    exit 1
  fi
  if [ ! -d "/etc/svdir/available/${service}" ]
  then
    echo "ERROR: service ${service} not found"
    exit 1
  fi
}

case "${action}" in
  start)
    check_enabled_service
    /usr/bin/printf "u" >/etc/svdir/enabled/${service}/control
    /usr/bin/cat /etc/svdir/enabled/${service}/status
    ;;
  stop)
    check_enabled_service
    /usr/bin/printf "d" >/etc/svdir/enabled/${service}/control
    /usr/bin/cat /etc/svdir/enabled/${service}/status
    ;;
  status|st)
    check_service
    if [ -f "/etc/svdir/enabled/${service}/status" ]
    then
      /usr/bin/cat /etc/svdir/enabled/${service}/status
    else
      if [ -d "/etc/svdir/available/${service}" ]
      then
        echo "disabled"
      else
        echo "not found"
      fi
    fi
    ;;
  enable)
    if [ ! -d "/etc/svdir/enabled/${service}" ]
    then
      if [ ! -d "/etc/svdir/available/${service}" ]
      then
        echo "ERROR: service ${service} not found"
        exit 1
      fi
      /usr/bin/ln -s /etc/svdir/available/${service} /etc/svdir/enabled/${service}
    fi
    ;;
  disable)
    check_enabled_service
    /usr/bin/printf "d" >/etc/svdir/enabled/${service}/control
    /usr/bin/rm -rf /etc/svdir/enabled/${service}
    ;;
  list)
    ls -1 /etc/svdir/enabled | sort
    ;;
  *)
    echo "Usage: sv <status | start | stop | enable | disable> service"
    echo "   or: sv list"
    ;;
esac
